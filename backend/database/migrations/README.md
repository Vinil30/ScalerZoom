# Migrations

Phase 1 uses SQLAlchemy metadata creation for SQLite setup.

When this project moves beyond the assignment foundation, this directory is reserved for Alembic migrations so schema changes can be versioned, reviewed, and deployed safely.
